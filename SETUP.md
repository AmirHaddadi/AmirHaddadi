# راه‌اندازی صفحهٔ پروفایل گیت‌هاب

## چرا این روش کار می‌کند
گیت‌هاب داخل `README.md` هر `<style>` و `<script>` و انیمیشن CSS را حذف می‌کند.
**اما** یک فایل SVG که با `<img src="...">` بارگذاری شود، `<style>` و `@keyframes` داخل خودش را نگه می‌دارد و در مرورگر **انیمیت می‌شود**.
بنر `assets/hero.svg` دقیقاً همین است: پایپ‌لاین CI/CD متحرک، گرید متحرک، افکت تایپ، و ذرهٔ داده‌ای که روی مسیر حرکت می‌کند.

## مراحل

1. **ساخت ریپو مخصوص:** یک ریپوی public بساز که نامش **دقیقاً برابر یوزرنیم گیت‌هابت** باشد
   (مثلاً یوزرنیم `AmirHaddadi` → ریپو `AmirHaddadi/AmirHaddadi`). هنگام ساخت تیک «Add a README file» را بزن.

2. **آپلود فایل‌ها:** این‌ها را در ریشهٔ ریپو بگذار:
   - `README.md`
   - `assets/hero.svg`
   - `.github/workflows/snake.yml`

3. **مقادیر از قبل پر شده:** یوزرنیم `AmirHaddadi`، ایمیل `hadaddi93@gmail.com`، نام «Amir Haddadi» و لینک مار همگی ست شده‌اند. اگر ایمیل تماس دیگری می‌خواهی فقط خط `mailto:` در `README.md` را عوض کن. فایل `LICENSE` (MIT) هم داخل ریپو هست.

4. **فعال‌سازی انیمیشن مار (snake):**
   - Settings → Actions → General → Workflow permissions → **Read and write** را روشن کن
   - تب Actions → workflow «generate snake» → **Run workflow**
   - بعد از اجرا، برنچ `output` ساخته می‌شود و تصویر مار در README ظاهر می‌شود

5. **کامیت → صفحهٔ اصلی پروفایلت خودکار همین README را رندر می‌کند.**

## چه چیزی در صفحهٔ اول دیده می‌شود (بالا به پایین)
بنر متحرک SVG → بج‌ها (followers / views / hire me) → متن تایپ‌شونده →
ناوبری سریع → درباره من → Tech Stack (آیکون‌ها) → جدول پروژه‌ها →
کارت‌های آمار + استریک + گراف فعالیت + مار → رویکرد کاری.

## نکات
- همهٔ سرویس‌های آمار (github-readme-stats، streak-stats، activity-graph، skillicons، readme-typing-svg) رایگان و بدون نیاز به توکن‌اند.
- `hero.svg` به `prefers-reduced-motion` احترام می‌گذارد؛ برای کاربرانی که انیمیشن را خاموش کرده‌اند حالت ساکن نشان می‌دهد.
- می‌توانی رنگ‌ها را در بخش `<defs>` فایل SVG (گرادیان `accent`) عوض کنی.
